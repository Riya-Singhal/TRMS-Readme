<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>show all bookings</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <% if(s.length==0 ){%> 
        <p>the room is not booked for given date and time till now </p>
    <% } else{%>
    
    <% for(i=0;i<s.length;i++){ %>
    <p>blockno is :<%= s[i].blockno %><br>
        room no is:<%= s[i].roomno %> <br>
        capacity is:<%= s[i].capacity %><br>
        booker's login id is:<%= s[i].bookerloginid %><br>
        booker's teacher id is:<%= s[i].bookerteacherid %><br> 
        booker's name is :<%= s[i].bookername %><br>
        booker's contact no is:<%= s[i].bookercontactno %><br>
        date of booking is:<%= s[i].dateofbooking %><br>
        time of booking is:<% if(s[i].ninetotwelve==true){ %>
        9am-12pm 
        <%} else if(s[i].twelvetothree==true) {%>
        12pm-3pm
        <% }else if(s[i].threetosix==true) {%>
        3pm-6pm
        <% }else if(s[i].sixtoeight==true){ %>
        6pm-8pm 
        <%} %>         
    </p>
    <% }} %> 
    <form action="/home" method="GET">
        <button class="btn">GO TO HOME PAGE</button>
    </form>
    <div class="bottom-container">
        <p class="copyright">© 2021 TRMS</p>
        <a class="go-home " href="/home" target="_parent" rel="noreferrer noopener">go to home page</a>
        <a class="footer-link1" href="tel:9698650004">Contact us</a>
        <a class="footer-link2" href="">Feedback</a>
  
  
    </div>
  
</body>
</html>